{
  "name": "money-is-time",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun ./index.html",
    "build": "rm -rf dist && bun build ./index.html --outdir dist --minify && cp docs/screenshots/og-image.png dist/og-image.png"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  }
}
